# URL do banco (ex.: PostgreSQL no Neon / Supabase / Render)
DATABASE_URL="postgresql://usuario:senha@host:5432/julia_lashes?schema=public"

# Segredo JWT forte
JWT_SECRET="uma_senha_bem_grande_e_secreta_aqui"

# Opção para configurar CORS (domínio do front)
CORS_ORIGIN="http://localhost:3000"
